━━━━━━━━━━━━━━━━━━━━━━ document_summary ━━━━━━━━━━━━━━━━━━━━━━
  README.md > (summary)
  lines 1–23  │  126 tokens  │  chunk 1 of 3
──────────────────────────────────────────────────────────────
# Sample Tkinter Tabbed App (Ingestion Test Fixture)

This is a small Python/Tkinter app intended as a **clean ingestion target** for testing a data store pipeline.

It includes:

- `app.py` as the application entry point
- `ui.py` as the UI module
- A custom tab system with:
  - **3 default tabs**
  - **inline close buttons (`×`)**
  - **inline new tab button (`+`)**
  - tab content areas with controls and text

---

## File Structure

```text
sample_tk_ingest_app/
├── app.py
├── ui.py
└── README.md
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ section ━━━━━━━━━━━━━━━━━━━━━━━━━━
  README.md > Sample Tkinter Tabbed App (Ingestion Test Fixture)
  lines 1–16  │  105 tokens  │  chunk 2 of 3
──────────────────────────────────────────────────────────────
# Sample Tkinter Tabbed App (Ingestion Test Fixture)

This is a small Python/Tkinter app intended as a **clean ingestion target** for testing a data store pipeline.

It includes:

- `app.py` as the application entry point
- `ui.py` as the UI module
- A custom tab system with:
  - **3 default tabs**
  - **inline close buttons (`×`)**
  - **inline new tab button (`+`)**
  - tab content areas with controls and text

---

══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━ subsection ━━━━━━━━━━━━━━━━━━━━━━━━━
  README.md > Sample Tkinter Tabbed App (Ingestion Test Fixture) > File Structure
  lines 17–23  │  21 tokens  │  chunk 3 of 3
──────────────────────────────────────────────────────────────
## File Structure

```text
sample_tk_ingest_app/
├── app.py
├── ui.py
└── README.md
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ import_block ━━━━━━━━━━━━━━━━━━━━━━━━
  app.py > imports
  lines 1–2  │  11 tokens  │  chunk 1 of 2
──────────────────────────────────────────────────────────────
import tkinter as tk
from ui import build_ui
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  app.py > main()
  lines 5–8  │  19 tokens  │  chunk 2 of 2
──────────────────────────────────────────────────────────────
def main() -> None:
    root = tk.Tk()
    build_ui(root)
    root.mainloop()
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━ document_summary ━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt > (summary)
  lines 1–33  │  250 tokens  │  chunk 1 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━ document_summary ━━━━━━━━━━━━━━━━━━━━━━
  README.md > (summary)
  lines 1–23  │  126 tokens  │  chunk 1 of 3
──────────────────────────────────────────────────────────────
# Sample Tkinter Tabbed App (Ingestion Test Fixture)

This is a small Python/Tkinter app intended as a **clean ingestion target** for testing a data store pipeline.

It includes:

- `app.py` as the application entry point
- `ui.py` as the UI module
- A custom tab system with:
  - **3 default tabs**
  - **inline close buttons (`×`)**
  - **inline new tab button (`+`)**
  - tab content areas with controls and text

---

## File Structure

```text
sample_tk_ingest_app/
├── app.py
├── ui.py
└── README.md
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ section ━━━━━━━━━━━━━━━━━━━━━━━━━━
  README.md > Sample Tkinter Tabbed App (Ingestion Test Fixture)
  lines 1–16  │  105 tokens  │  chunk 2 of 3
──────────────────────────────────────────────────────────────
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 1–5  │  62 tokens  │  chunk 2 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━ document_summary ━━━━━━━━━━━━━━━━━━━━━━
  README.md > (summary)
  lines 1–23  │  126 tokens  │  chunk 1 of 3
──────────────────────────────────────────────────────────────
# Sample Tkinter Tabbed App (Ingestion Test Fixture)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 7–7  │  28 tokens  │  chunk 3 of 75
──────────────────────────────────────────────────────────────
This is a small Python/Tkinter app intended as a **clean ingestion target** for testing a data store pipeline.
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 9–9  │  3 tokens  │  chunk 4 of 75
──────────────────────────────────────────────────────────────
It includes:
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 11–17  │  59 tokens  │  chunk 5 of 75
──────────────────────────────────────────────────────────────
- `app.py` as the application entry point
- `ui.py` as the UI module
- A custom tab system with:
  - **3 default tabs**
  - **inline close buttons (`×`)**
  - **inline new tab button (`+`)**
  - tab content areas with controls and text
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 19–19  │  1 tokens  │  chunk 6 of 75
──────────────────────────────────────────────────────────────
---
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 21–21  │  4 tokens  │  chunk 7 of 75
──────────────────────────────────────────────────────────────
## File Structure
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 23–28  │  32 tokens  │  chunk 8 of 75
──────────────────────────────────────────────────────────────
```text
sample_tk_ingest_app/
├── app.py
├── ui.py
└── README.md
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 30–34  │  72 tokens  │  chunk 9 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━━━ section ━━━━━━━━━━━━━━━━━━━━━━━━━━
  README.md > Sample Tkinter Tabbed App (Ingestion Test Fixture)
  lines 1–16  │  105 tokens  │  chunk 2 of 3
──────────────────────────────────────────────────────────────
# Sample Tkinter Tabbed App (Ingestion Test Fixture)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 36–36  │  28 tokens  │  chunk 10 of 75
──────────────────────────────────────────────────────────────
This is a small Python/Tkinter app intended as a **clean ingestion target** for testing a data store pipeline.
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 38–38  │  3 tokens  │  chunk 11 of 75
──────────────────────────────────────────────────────────────
It includes:
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 40–46  │  59 tokens  │  chunk 12 of 75
──────────────────────────────────────────────────────────────
- `app.py` as the application entry point
- `ui.py` as the UI module
- A custom tab system with:
  - **3 default tabs**
  - **inline close buttons (`×`)**
  - **inline new tab button (`+`)**
  - tab content areas with controls and text
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 48–48  │  1 tokens  │  chunk 13 of 75
──────────────────────────────────────────────────────────────
---
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 50–50  │  16 tokens  │  chunk 14 of 75
──────────────────────────────────────────────────────────────
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 52–56  │  68 tokens  │  chunk 15 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━━ subsection ━━━━━━━━━━━━━━━━━━━━━━━━━
  README.md > Sample Tkinter Tabbed App (Ingestion Test Fixture) > File Structure
  lines 17–23  │  21 tokens  │  chunk 3 of 3
──────────────────────────────────────────────────────────────
## File Structure
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 58–63  │  32 tokens  │  chunk 16 of 75
──────────────────────────────────────────────────────────────
```text
sample_tk_ingest_app/
├── app.py
├── ui.py
└── README.md
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 65–71  │  74 tokens  │  chunk 17 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ import_block ━━━━━━━━━━━━━━━━━━━━━━━━
  app.py > imports
  lines 1–2  │  11 tokens  │  chunk 1 of 2
──────────────────────────────────────────────────────────────
import tkinter as tk
from ui import build_ui
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 73–81  │  82 tokens  │  chunk 18 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  app.py > main()
  lines 5–8  │  19 tokens  │  chunk 2 of 2
──────────────────────────────────────────────────────────────
def main() -> None:
    root = tk.Tk()
    build_ui(root)
    root.mainloop()
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 83–89  │  74 tokens  │  chunk 19 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ import_block ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > imports
  lines 1–2  │  11 tokens  │  chunk 1 of 14
──────────────────────────────────────────────────────────────
import tkinter as tk
from tkinter import ttk
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 91–97  │  67 tokens  │  chunk 20 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━━ class_def ━━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI
  lines 5–16  │  73 tokens  │  chunk 2 of 14
──────────────────────────────────────────────────────────────
class TabbedUI:
    """
    Simple custom tabbed UI for ingestion/testing.
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 99–105  │  54 tokens  │  chunk 21 of 75
──────────────────────────────────────────────────────────────
    Features:
    - Custom tab strip (not ttk.Notebook) so we can place:
      - per-tab close buttons (×)
      - inline new tab button (+)
    - Starts with 3 tabs
    - Each tab has a small content panel
    """
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 107–107  │  16 tokens  │  chunk 22 of 75
──────────────────────────────────────────────────────────────
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 109–117  │  98 tokens  │  chunk 23 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > __init__()
  lines 17–35  │  134 tokens  │  chunk 3 of 14
──────────────────────────────────────────────────────────────
    def __init__(self, root: tk.Tk):
        self.root = root
        self.root.title("Sample Ingestion UI")
        self.root.geometry("900x600")
        self.root.minsize(700, 450)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 119–124  │  46 tokens  │  chunk 24 of 75
──────────────────────────────────────────────────────────────
        # Theme/style (kept simple and stdlib-only)
        self.style = ttk.Style()
        try:
            self.style.theme_use("clam")
        except tk.TclError:
            pass
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 126–128  │  26 tokens  │  chunk 25 of 75
──────────────────────────────────────────────────────────────
        self._tab_counter = 0
        self._active_tab_id = None
        self._tabs = []  # list[dict]
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 130–132  │  32 tokens  │  chunk 26 of 75
──────────────────────────────────────────────────────────────
        self._build_layout()
        self._create_initial_tabs()
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 134–140  │  88 tokens  │  chunk 27 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _build_layout()
  lines 40–68  │  289 tokens  │  chunk 4 of 14
──────────────────────────────────────────────────────────────
    def _build_layout(self) -> None:
        self.root.grid_rowconfigure(1, weight=1)
        self.root.grid_columnconfigure(0, weight=1)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 142–145  │  52 tokens  │  chunk 28 of 75
──────────────────────────────────────────────────────────────
        # Top bar container
        self.top_frame = ttk.Frame(self.root, padding=(8, 8, 8, 4))
        self.top_frame.grid(row=0, column=0, sticky="ew")
        self.top_frame.grid_columnconfigure(0, weight=1)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 147–149  │  46 tokens  │  chunk 29 of 75
──────────────────────────────────────────────────────────────
        # Tab strip (left side) + utility area (right side if needed later)
        self.tab_strip = ttk.Frame(self.top_frame)
        self.tab_strip.grid(row=0, column=0, sticky="w")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 151–155  │  73 tokens  │  chunk 30 of 75
──────────────────────────────────────────────────────────────
        # Content area
        self.content_container = ttk.Frame(self.root, padding=(8, 4, 8, 8))
        self.content_container.grid(row=1, column=0, sticky="nsew")
        self.content_container.grid_rowconfigure(0, weight=1)
        self.content_container.grid_columnconfigure(0, weight=1)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 157–167  │  97 tokens  │  chunk 31 of 75
──────────────────────────────────────────────────────────────
        # Status bar
        self.status_var = tk.StringVar(value="Ready")
        self.status_bar = ttk.Label(
            self.root,
            textvariable=self.status_var,
            anchor="w",
            padding=(8, 4),
            relief="groove"
        )
        self.status_bar.grid(row=2, column=0, sticky="ew")
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 169–178  │  120 tokens  │  chunk 32 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _create_initial_tabs()
  lines 70–74  │  49 tokens  │  chunk 5 of 14
──────────────────────────────────────────────────────────────
    def _create_initial_tabs(self) -> None:
        self.add_tab(title="Tab 1")
        self.add_tab(title="Tab 2")
        self.add_tab(title="Tab 3")
        self.select_tab(self._tabs[0]["id"])
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 180–187  │  100 tokens  │  chunk 33 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > add_tab()
  lines 79–100  │  190 tokens  │  chunk 6 of 14
──────────────────────────────────────────────────────────────
    def add_tab(self, title: str | None = None) -> None:
        self._tab_counter += 1
        tab_id = f"tab_{self._tab_counter}"
        tab_title = title or f"Tab {self._tab_counter}"
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 189–192  │  54 tokens  │  chunk 34 of 75
──────────────────────────────────────────────────────────────
        # Create content frame for this tab
        content = ttk.Frame(self.content_container, padding=12)
        content.grid(row=0, column=0, sticky="nsew")
        content.grid_remove()  # hidden until selected
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 194–195  │  26 tokens  │  chunk 35 of 75
──────────────────────────────────────────────────────────────
        # Fill tab content with sample widgets
        self._build_tab_content(content, tab_title, tab_id)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 197–201  │  32 tokens  │  chunk 36 of 75
──────────────────────────────────────────────────────────────
        self._tabs.append({
            "id": tab_id,
            "title": tab_title,
            "content": content,
        })
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 203–206  │  44 tokens  │  chunk 37 of 75
──────────────────────────────────────────────────────────────
        self._render_tab_strip()
        self.select_tab(tab_id)
        self.status_var.set(f"Added {tab_title}")
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 208–216  │  113 tokens  │  chunk 38 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > close_tab()
  lines 102–131  │  250 tokens  │  chunk 7 of 14
──────────────────────────────────────────────────────────────
    def close_tab(self, tab_id: str) -> None:
        if len(self._tabs) == 1:
            # Keep at least one tab around for usability
            self.status_var.set("Cannot close the last tab. Add a new tab first.")
            return
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 218–219  │  14 tokens  │  chunk 39 of 75
──────────────────────────────────────────────────────────────
        closing_index = None
        closing_tab = None
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 221–225  │  42 tokens  │  chunk 40 of 75
──────────────────────────────────────────────────────────────
        for i, tab in enumerate(self._tabs):
            if tab["id"] == tab_id:
                closing_index = i
                closing_tab = tab
                break
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 227–228  │  12 tokens  │  chunk 41 of 75
──────────────────────────────────────────────────────────────
        if closing_tab is None:
            return
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 230–232  │  28 tokens  │  chunk 42 of 75
──────────────────────────────────────────────────────────────
        # Destroy content frame
        closing_tab["content"].destroy()
        self._tabs.pop(closing_index)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 234–237  │  60 tokens  │  chunk 43 of 75
──────────────────────────────────────────────────────────────
        # Pick next active tab (same index if possible, else previous)
        if self._active_tab_id == tab_id:
            new_index = min(closing_index, len(self._tabs) - 1)
            self._active_tab_id = self._tabs[new_index]["id"]
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 239–242  │  47 tokens  │  chunk 44 of 75
──────────────────────────────────────────────────────────────
        self._render_tab_strip()
        self._show_active_tab()
        self.status_var.set(f"Closed {closing_tab['title']}")
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 244–253  │  110 tokens  │  chunk 45 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > select_tab()
  lines 133–142  │  87 tokens  │  chunk 8 of 14
──────────────────────────────────────────────────────────────
    def select_tab(self, tab_id: str) -> None:
        if not any(t["id"] == tab_id for t in self._tabs):
            return
        self._active_tab_id = tab_id
        self._render_tab_strip()
        self._show_active_tab()
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 255–258  │  46 tokens  │  chunk 46 of 75
──────────────────────────────────────────────────────────────
        active = self._get_tab(tab_id)
        if active:
            self.status_var.set(f"Viewing {active['title']}")
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 260–267  │  92 tokens  │  chunk 47 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _render_tab_strip()
  lines 147–188  │  338 tokens  │  chunk 9 of 14
──────────────────────────────────────────────────────────────
    def _render_tab_strip(self) -> None:
        # Clear strip
        for child in self.tab_strip.winfo_children():
            child.destroy()
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 269–271  │  31 tokens  │  chunk 48 of 75
──────────────────────────────────────────────────────────────
        # Rebuild all tab "buttons"
        for tab in self._tabs:
            is_active = (tab["id"] == self._active_tab_id)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 273–274  │  31 tokens  │  chunk 49 of 75
──────────────────────────────────────────────────────────────
            tab_frame = ttk.Frame(self.tab_strip, padding=(0, 0, 4, 0))
            tab_frame.pack(side="left", anchor="w")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 276–283  │  73 tokens  │  chunk 50 of 75
──────────────────────────────────────────────────────────────
            # Tab button (label area)
            btn_text = f" {tab['title']} "
            tab_btn = ttk.Button(
                tab_frame,
                text=btn_text,
                command=lambda tid=tab["id"]: self.select_tab(tid)
            )
            tab_btn.pack(side="left")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 285–287  │  36 tokens  │  chunk 51 of 75
──────────────────────────────────────────────────────────────
            # Give active tab a visual hint by disabling its main button
            if is_active:
                tab_btn.state(["disabled"])
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 289–296  │  72 tokens  │  chunk 52 of 75
──────────────────────────────────────────────────────────────
            # Close button inline with tab
            close_btn = ttk.Button(
                tab_frame,
                text="×",
                width=3,
                command=lambda tid=tab["id"]: self.close_tab(tid)
            )
            close_btn.pack(side="left", padx=(2, 0))
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 298–306  │  72 tokens  │  chunk 53 of 75
──────────────────────────────────────────────────────────────
        # Inline "New Tab" button
        add_btn = ttk.Button(
            self.tab_strip,
            text="+",
            width=3,
            command=self.add_tab
        )
        add_btn.pack(side="left", padx=(6, 0))
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 308–314  │  83 tokens  │  chunk 54 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _show_active_tab()
  lines 190–196  │  55 tokens  │  chunk 10 of 14
──────────────────────────────────────────────────────────────
    def _show_active_tab(self) -> None:
        for tab in self._tabs:
            tab["content"].grid_remove()
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 316–319  │  42 tokens  │  chunk 55 of 75
──────────────────────────────────────────────────────────────
        active = self._get_tab(self._active_tab_id)
        if active:
            active["content"].grid()
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 321–327  │  101 tokens  │  chunk 56 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _build_tab_content()
  lines 201–246  │  429 tokens  │  chunk 11 of 14
──────────────────────────────────────────────────────────────
    def _build_tab_content(self, parent: ttk.Frame, title: str, tab_id: str) -> None:
        parent.grid_columnconfigure(0, weight=1)
        parent.grid_rowconfigure(3, weight=1)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 329–330  │  35 tokens  │  chunk 57 of 75
──────────────────────────────────────────────────────────────
        title_label = ttk.Label(parent, text=title, font=("TkDefaultFont", 12, "bold"))
        title_label.grid(row=0, column=0, sticky="w")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 332–333  │  34 tokens  │  chunk 58 of 75
──────────────────────────────────────────────────────────────
        meta_label = ttk.Label(parent, text=f"Internal ID: {tab_id}")
        meta_label.grid(row=1, column=0, sticky="w", pady=(2, 10))
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 335–336  │  26 tokens  │  chunk 59 of 75
──────────────────────────────────────────────────────────────
        controls = ttk.Frame(parent)
        controls.grid(row=2, column=0, sticky="ew", pady=(0, 10))
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 338–342  │  40 tokens  │  chunk 60 of 75
──────────────────────────────────────────────────────────────
        ttk.Button(
            controls,
            text="Rename Tab",
            command=lambda: self._rename_tab_dialog(tab_id)
        ).pack(side="left")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 344–348  │  37 tokens  │  chunk 61 of 75
──────────────────────────────────────────────────────────────
        ttk.Button(
            controls,
            text="Add New Tab",
            command=self.add_tab
        ).pack(side="left", padx=(8, 0))
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 350–354  │  42 tokens  │  chunk 62 of 75
──────────────────────────────────────────────────────────────
        ttk.Button(
            controls,
            text="Close This Tab",
            command=lambda: self.close_tab(tab_id)
        ).pack(side="left", padx=(8, 0))
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 356–358  │  46 tokens  │  chunk 63 of 75
──────────────────────────────────────────────────────────────
        # A text area gives your ingestor something realistic to parse
        text_box = tk.Text(parent, wrap="word", height=16)
        text_box.grid(row=3, column=0, sticky="nsew")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 360–371  │  136 tokens  │  chunk 64 of 75
──────────────────────────────────────────────────────────────
        sample_text = (
            f"This is {title}.\n\n"
            "This sample app is intentionally small and structured for ingestion tests.\n"
            "It contains:\n"
            "- a custom tab strip\n"
            "- per-tab close controls\n"
            "- an inline '+' button for new tabs\n"
            "- dynamic content panels\n\n"
            "You can rename, add, and close tabs to generate state changes."
        )
        text_box.insert("1.0", sample_text)
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 373–382  │  106 tokens  │  chunk 65 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _get_tab()
  lines 251–255  │  38 tokens  │  chunk 12 of 14
──────────────────────────────────────────────────────────────
    def _get_tab(self, tab_id: str):
        for tab in self._tabs:
            if tab["id"] == tab_id:
                return tab
        return None
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 384–391  │  88 tokens  │  chunk 66 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _rename_tab_dialog()
  lines 257–294  │  338 tokens  │  chunk 13 of 14
──────────────────────────────────────────────────────────────
    def _rename_tab_dialog(self, tab_id: str) -> None:
        tab = self._get_tab(tab_id)
        if not tab:
            return
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 393–397  │  44 tokens  │  chunk 67 of 75
──────────────────────────────────────────────────────────────
        dialog = tk.Toplevel(self.root)
        dialog.title("Rename Tab")
        dialog.transient(self.root)
        dialog.grab_set()
        dialog.resizable(False, False)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 399–400  │  22 tokens  │  chunk 68 of 75
──────────────────────────────────────────────────────────────
        frame = ttk.Frame(dialog, padding=12)
        frame.pack(fill="both", expand=True)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 402–402  │  20 tokens  │  chunk 69 of 75
──────────────────────────────────────────────────────────────
        ttk.Label(frame, text="New tab name:").grid(row=0, column=0, sticky="w")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 404–408  │  61 tokens  │  chunk 70 of 75
──────────────────────────────────────────────────────────────
        name_var = tk.StringVar(value=tab["title"])
        entry = ttk.Entry(frame, textvariable=name_var, width=30)
        entry.grid(row=1, column=0, sticky="ew", pady=(4, 10))
        entry.focus_set()
        entry.select_range(0, "end")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 410–411  │  20 tokens  │  chunk 71 of 75
──────────────────────────────────────────────────────────────
        btns = ttk.Frame(frame)
        btns.grid(row=2, column=0, sticky="e")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 413–419  │  66 tokens  │  chunk 72 of 75
──────────────────────────────────────────────────────────────
        def save():
            new_name = name_var.get().strip()
            if new_name:
                tab["title"] = new_name
                self._render_tab_strip()
                self.status_var.set(f"Renamed tab to {new_name}")
            dialog.destroy()
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 421–422  │  41 tokens  │  chunk 73 of 75
──────────────────────────────────────────────────────────────
        ttk.Button(btns, text="Cancel", command=dialog.destroy).pack(side="left")
        ttk.Button(btns, text="Save", command=save).pack(side="left", padx=(6, 0))
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 424–426  │  44 tokens  │  chunk 74 of 75
──────────────────────────────────────────────────────────────
        dialog.bind("<Return>", lambda _e: save())
        dialog.bind("<Escape>", lambda _e: dialog.destroy())
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━ document ━━━━━━━━━━━━━━━━━━━━━━━━━━
  chunk_stream.txt
  lines 428–434  │  81 tokens  │  chunk 75 of 75
──────────────────────────────────────────────────────────────
━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > build_ui()
  lines 297–298  │  16 tokens  │  chunk 14 of 14
──────────────────────────────────────────────────────────────
def build_ui(root: tk.Tk) -> TabbedUI:
    return TabbedUI(root)
══════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ import_block ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > imports
  lines 1–2  │  11 tokens  │  chunk 1 of 14
──────────────────────────────────────────────────────────────
import tkinter as tk
from tkinter import ttk
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━ class_def ━━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI
  lines 5–16  │  73 tokens  │  chunk 2 of 14
──────────────────────────────────────────────────────────────
class TabbedUI:
    """
    Simple custom tabbed UI for ingestion/testing.

    Features:
    - Custom tab strip (not ttk.Notebook) so we can place:
      - per-tab close buttons (×)
      - inline new tab button (+)
    - Starts with 3 tabs
    - Each tab has a small content panel
    """

══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > __init__()
  lines 17–35  │  134 tokens  │  chunk 3 of 14
──────────────────────────────────────────────────────────────
    def __init__(self, root: tk.Tk):
        self.root = root
        self.root.title("Sample Ingestion UI")
        self.root.geometry("900x600")
        self.root.minsize(700, 450)

        # Theme/style (kept simple and stdlib-only)
        self.style = ttk.Style()
        try:
            self.style.theme_use("clam")
        except tk.TclError:
            pass

        self._tab_counter = 0
        self._active_tab_id = None
        self._tabs = []  # list[dict]

        self._build_layout()
        self._create_initial_tabs()
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _build_layout()
  lines 40–68  │  289 tokens  │  chunk 4 of 14
──────────────────────────────────────────────────────────────
    def _build_layout(self) -> None:
        self.root.grid_rowconfigure(1, weight=1)
        self.root.grid_columnconfigure(0, weight=1)

        # Top bar container
        self.top_frame = ttk.Frame(self.root, padding=(8, 8, 8, 4))
        self.top_frame.grid(row=0, column=0, sticky="ew")
        self.top_frame.grid_columnconfigure(0, weight=1)

        # Tab strip (left side) + utility area (right side if needed later)
        self.tab_strip = ttk.Frame(self.top_frame)
        self.tab_strip.grid(row=0, column=0, sticky="w")

        # Content area
        self.content_container = ttk.Frame(self.root, padding=(8, 4, 8, 8))
        self.content_container.grid(row=1, column=0, sticky="nsew")
        self.content_container.grid_rowconfigure(0, weight=1)
        self.content_container.grid_columnconfigure(0, weight=1)

        # Status bar
        self.status_var = tk.StringVar(value="Ready")
        self.status_bar = ttk.Label(
            self.root,
            textvariable=self.status_var,
            anchor="w",
            padding=(8, 4),
            relief="groove"
        )
        self.status_bar.grid(row=2, column=0, sticky="ew")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _create_initial_tabs()
  lines 70–74  │  49 tokens  │  chunk 5 of 14
──────────────────────────────────────────────────────────────
    def _create_initial_tabs(self) -> None:
        self.add_tab(title="Tab 1")
        self.add_tab(title="Tab 2")
        self.add_tab(title="Tab 3")
        self.select_tab(self._tabs[0]["id"])
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > add_tab()
  lines 79–100  │  190 tokens  │  chunk 6 of 14
──────────────────────────────────────────────────────────────
    def add_tab(self, title: str | None = None) -> None:
        self._tab_counter += 1
        tab_id = f"tab_{self._tab_counter}"
        tab_title = title or f"Tab {self._tab_counter}"

        # Create content frame for this tab
        content = ttk.Frame(self.content_container, padding=12)
        content.grid(row=0, column=0, sticky="nsew")
        content.grid_remove()  # hidden until selected

        # Fill tab content with sample widgets
        self._build_tab_content(content, tab_title, tab_id)

        self._tabs.append({
            "id": tab_id,
            "title": tab_title,
            "content": content,
        })

        self._render_tab_strip()
        self.select_tab(tab_id)
        self.status_var.set(f"Added {tab_title}")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > close_tab()
  lines 102–131  │  250 tokens  │  chunk 7 of 14
──────────────────────────────────────────────────────────────
    def close_tab(self, tab_id: str) -> None:
        if len(self._tabs) == 1:
            # Keep at least one tab around for usability
            self.status_var.set("Cannot close the last tab. Add a new tab first.")
            return

        closing_index = None
        closing_tab = None

        for i, tab in enumerate(self._tabs):
            if tab["id"] == tab_id:
                closing_index = i
                closing_tab = tab
                break

        if closing_tab is None:
            return

        # Destroy content frame
        closing_tab["content"].destroy()
        self._tabs.pop(closing_index)

        # Pick next active tab (same index if possible, else previous)
        if self._active_tab_id == tab_id:
            new_index = min(closing_index, len(self._tabs) - 1)
            self._active_tab_id = self._tabs[new_index]["id"]

        self._render_tab_strip()
        self._show_active_tab()
        self.status_var.set(f"Closed {closing_tab['title']}")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > select_tab()
  lines 133–142  │  87 tokens  │  chunk 8 of 14
──────────────────────────────────────────────────────────────
    def select_tab(self, tab_id: str) -> None:
        if not any(t["id"] == tab_id for t in self._tabs):
            return
        self._active_tab_id = tab_id
        self._render_tab_strip()
        self._show_active_tab()

        active = self._get_tab(tab_id)
        if active:
            self.status_var.set(f"Viewing {active['title']}")
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _render_tab_strip()
  lines 147–188  │  338 tokens  │  chunk 9 of 14
──────────────────────────────────────────────────────────────
    def _render_tab_strip(self) -> None:
        # Clear strip
        for child in self.tab_strip.winfo_children():
            child.destroy()

        # Rebuild all tab "buttons"
        for tab in self._tabs:
            is_active = (tab["id"] == self._active_tab_id)

            tab_frame = ttk.Frame(self.tab_strip, padding=(0, 0, 4, 0))
            tab_frame.pack(side="left", anchor="w")

            # Tab button (label area)
            btn_text = f" {tab['title']} "
            tab_btn = ttk.Button(
                tab_frame,
                text=btn_text,
                command=lambda tid=tab["id"]: self.select_tab(tid)
            )
            tab_btn.pack(side="left")

            # Give active tab a visual hint by disabling its main button
            if is_active:
                tab_btn.state(["disabled"])

            # Close button inline with tab
            close_btn = ttk.Button(
                tab_frame,
                text="×",
                width=3,
                command=lambda tid=tab["id"]: self.close_tab(tid)
            )
            close_btn.pack(side="left", padx=(2, 0))

        # Inline "New Tab" button
        add_btn = ttk.Button(
            self.tab_strip,
            text="+",
            width=3,
            command=self.add_tab
        )
        add_btn.pack(side="left", padx=(6, 0))
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _show_active_tab()
  lines 190–196  │  55 tokens  │  chunk 10 of 14
──────────────────────────────────────────────────────────────
    def _show_active_tab(self) -> None:
        for tab in self._tabs:
            tab["content"].grid_remove()

        active = self._get_tab(self._active_tab_id)
        if active:
            active["content"].grid()
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _build_tab_content()
  lines 201–246  │  429 tokens  │  chunk 11 of 14
──────────────────────────────────────────────────────────────
    def _build_tab_content(self, parent: ttk.Frame, title: str, tab_id: str) -> None:
        parent.grid_columnconfigure(0, weight=1)
        parent.grid_rowconfigure(3, weight=1)

        title_label = ttk.Label(parent, text=title, font=("TkDefaultFont", 12, "bold"))
        title_label.grid(row=0, column=0, sticky="w")

        meta_label = ttk.Label(parent, text=f"Internal ID: {tab_id}")
        meta_label.grid(row=1, column=0, sticky="w", pady=(2, 10))

        controls = ttk.Frame(parent)
        controls.grid(row=2, column=0, sticky="ew", pady=(0, 10))

        ttk.Button(
            controls,
            text="Rename Tab",
            command=lambda: self._rename_tab_dialog(tab_id)
        ).pack(side="left")

        ttk.Button(
            controls,
            text="Add New Tab",
            command=self.add_tab
        ).pack(side="left", padx=(8, 0))

        ttk.Button(
            controls,
            text="Close This Tab",
            command=lambda: self.close_tab(tab_id)
        ).pack(side="left", padx=(8, 0))

        # A text area gives your ingestor something realistic to parse
        text_box = tk.Text(parent, wrap="word", height=16)
        text_box.grid(row=3, column=0, sticky="nsew")

        sample_text = (
            f"This is {title}.\n\n"
            "This sample app is intentionally small and structured for ingestion tests.\n"
            "It contains:\n"
            "- a custom tab strip\n"
            "- per-tab close controls\n"
            "- an inline '+' button for new tabs\n"
            "- dynamic content panels\n\n"
            "You can rename, add, and close tabs to generate state changes."
        )
        text_box.insert("1.0", sample_text)
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _get_tab()
  lines 251–255  │  38 tokens  │  chunk 12 of 14
──────────────────────────────────────────────────────────────
    def _get_tab(self, tab_id: str):
        for tab in self._tabs:
            if tab["id"] == tab_id:
                return tab
        return None
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > class TabbedUI > _rename_tab_dialog()
  lines 257–294  │  338 tokens  │  chunk 13 of 14
──────────────────────────────────────────────────────────────
    def _rename_tab_dialog(self, tab_id: str) -> None:
        tab = self._get_tab(tab_id)
        if not tab:
            return

        dialog = tk.Toplevel(self.root)
        dialog.title("Rename Tab")
        dialog.transient(self.root)
        dialog.grab_set()
        dialog.resizable(False, False)

        frame = ttk.Frame(dialog, padding=12)
        frame.pack(fill="both", expand=True)

        ttk.Label(frame, text="New tab name:").grid(row=0, column=0, sticky="w")

        name_var = tk.StringVar(value=tab["title"])
        entry = ttk.Entry(frame, textvariable=name_var, width=30)
        entry.grid(row=1, column=0, sticky="ew", pady=(4, 10))
        entry.focus_set()
        entry.select_range(0, "end")

        btns = ttk.Frame(frame)
        btns.grid(row=2, column=0, sticky="e")

        def save():
            new_name = name_var.get().strip()
            if new_name:
                tab["title"] = new_name
                self._render_tab_strip()
                self.status_var.set(f"Renamed tab to {new_name}")
            dialog.destroy()

        ttk.Button(btns, text="Cancel", command=dialog.destroy).pack(side="left")
        ttk.Button(btns, text="Save", command=save).pack(side="left", padx=(6, 0))

        dialog.bind("<Return>", lambda _e: save())
        dialog.bind("<Escape>", lambda _e: dialog.destroy())
══════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━ function_def ━━━━━━━━━━━━━━━━━━━━━━━━
  ui.py > build_ui()
  lines 297–298  │  16 tokens  │  chunk 14 of 14
──────────────────────────────────────────────────────────────
def build_ui(root: tk.Tk) -> TabbedUI:
    return TabbedUI(root)
══════════════════════════════════════════════════════════════
